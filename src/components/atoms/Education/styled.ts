import { Box, Grid, Slider, Typography, styled } from '@mui/material';

export const StyledEducation = styled(Typography)`
  color: white;
  transition: none;
  font-size: 24px;
  font-family: 'my-font';
  &:hover {
    scale: 1.4;
  }
`;
