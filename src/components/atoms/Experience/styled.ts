import { Box, Grid, Slider, Typography, styled } from '@mui/material';

export const StyledExperience = styled(Typography)`
  color: white;
  transition: none;
  font-family: 'my-font';
  font-size: 24px;
  &:hover {
    scale: 1.2;
  }
`;
