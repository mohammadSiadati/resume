import { Box, Typography, styled, Grid } from '@mui/material';

export const StyledBtnCard = styled(Grid)`
  transition: all 0.3s;
  display: flex;
  text-align: center;
  align-items: center;
`;

export const StyledNameSkill = styled(Typography)`
  font-family: 'my-font';
  margin-left: 15px;
  font-size: 20px;
  color: white;
`;
